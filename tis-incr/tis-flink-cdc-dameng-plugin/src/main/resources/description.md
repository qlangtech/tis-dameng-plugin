
## DaMeng CDC Connector

The DaMeng CDC connector allows for reading incremental data from DaMeng database. This document describes how to setup the DaMeng CDC connector to run SQL queries against DaMeng databases.

